apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  labels:
    app: firstimage

spec:
  containers:
    - name: firstimage
      image: hoda122/firstimage
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 80
            
